{
  "name": "zayne-range-pond-invite",
  "version": "1.0.0",
  "private": true,
  "description": "Animated RSVP birthday invitation for Zayne's 13th birthday at Range Pond.",
  "scripts": {
    "start": "node local-server.js",
    "build": "node scripts/build-vercel.mjs",
    "check": "node --check local-server.js && node --check script.js && node --check scripts/build-vercel.mjs"
  },
  "keywords": [
    "birthday",
    "invitation",
    "rsvp",
    "static-site"
  ],
  "license": "UNLICENSED"
}
